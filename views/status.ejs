<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <title>Spotify Premium Status</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      text-align: center;
    }
    .status-container {
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      height: 100vh;
    }
    .status-message {
      font-size: 24px;
      font-weight: bold;
      margin-bottom: 20px;
    }
    .return-button {
      padding: 10px 20px;
      font-size: 16px;
      text-decoration: none;
      color: #fff;
      background-color: #0070f3;
      border-radius: 5px;
    }
  </style>
</head>
<body>
  <div class="status-container">
    <h1>Spotify Premium Status</h1>
    <div class="status-message">
      <% if (isPremium) { %>
        <p>You are a premium user.</p>
      <% } else { %>
        <p>You are not a premium user.</p>
      <% } %>
    </div>
    <a class="return-button" href="/">Return to Home</a>
  </div>
</body>
</html>
