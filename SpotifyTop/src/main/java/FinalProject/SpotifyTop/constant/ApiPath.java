// Defines a set of constants for API paths for Spotify application
package finalproject.spotifytop.constant;

public class ApiPath {

    // Represents the API path for the callback endpoint
    public static final String CALLBACK = "/callback";

    // Represents the API path for the logout endpoint
    public static final String LOGOUT = "/logout";

    // Represents the API path for the error endpoint
    public static final String ERROR = "/error";

    // Represents the API path for the redirect endpoint
    public static final String REDIRECT = "/redirect";

    // Represents the API path for the search endpoint
    public static final String SEARCH = "/search";

    // Represents the API path for the top artists endpoint
    public static final String TOP_ARTISTS = "/topArtists";

    // Represents the API path for the top tracks endpoint
    public static final String TOP_TRACKS = "/topTracks";

}
